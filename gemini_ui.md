# ROS2 Dashboard UI/UX 실화면 정밀 검수 보고서

**검수 일시:** 2026-08-26  
**검수 환경:** 실제 장비 Nginx HTTPS (`https://192.168.1.123/`), Chrome Headless (1920×1080 Full HD & 1440×900 Laptop 해상도)  
**검수 대상 페이지:** `/overview`, `/topics`, `/services`, `/actions`, `/nodes`, `/visualization`, `/alerts`, `/domains`, `/interface-lab` (총 9개 화면 전수 검수)

---

## 1. 종합 평가 요약

현재 ROS2 Dashboard는 다크 테마 기반의 일관된 톤앤매너, 실시간 WebSocket/QoS 상태 배지, 풍부한 진단 데이터 등 핵심 모니터링 기능이 매우 충실하게 구현되어 있습니다.  
그러나 **실제 브라우저 화면(특히 1440×900 표준 노트북 해상도 및 상세 패널 오픈 시)**에서 다음과 같은 구조적인 UX/UI 문제점들이 확인되었습니다:

1. **과도한 수직 여백 및 스크롤 강제**: 상단 요약 카드와 Alert 박스가 화면 높이의 50~70%를 차지하여 핵심 메인 테이블이 화면 밖으로 밀려남.
2. **패널 오픈 시 테이블 컬럼 겹침/가로 스크롤 버그**: 우측 상세 패널(390px)이 열리면 좌측 테이블의 긴 컬럼 헤더들이 겹치거나 가로 스크롤이 발생함.
3. **Visualization 시각화 겹침(Overlap)**: 노드 선택 시 토픽/서비스/액션 블록들이 서로 겹쳐서 글자가 가려짐.
4. **극단적인 가로 폭 활용 불균형**: Domains, Interface Lab 등의 단일 입력 필드가 1920px 전체 너비로 늘어나는 현상.
5. **텍스트 줄바꿈 및 영문/한글 혼용**: 단어 단위 줄바꿈 미적용(`Node 미` / `리보기`) 및 개발자 변수명 노출.

---

## 2. 개선 우선순위 로드맵

| 우선순위 | 영역 | 핵심 문제 및 개선안 | 영향도 |
| :--- | :--- | :--- | :--- |
| **P0 (긴급)** | **공통 / 반응형** | **상단 헤더/카드/Alert 높이 축소** (1440×900에서 테이블 첫 진입 시 즉시 노출되도록 컴팩트화) | 최상 (노트북 사용성) |
| **P0 (긴급)** | **Topics / Services / Actions** | **상세 패널 오픈 시 테이블 가로 스크롤 및 컬럼 헤더 겹침 버그 해결** | 최상 (데이터 가독성) |
| **P0 (긴급)** | **Visualization** | **노드 그래프 블록 오버랩(겹침) 현상 제거 및 자동 레이아웃(Dagre/ELK) 간격 조정** | 상 (시각화 기능) |
| **P1 (주요)** | **공통 / 상세 패널** | **상세 패널 중첩 스크롤(Double Scroll) 제거 및 아코디언 기본 노출/접힘 구조 최적화** | 상 (진단 편의성) |
| **P1 (주요)** | **Domains / Interface Lab** | **단일 텍스트/숫자 입력창 최대 너비(Max-Width) 제한 및 폼 레이아웃 정돈** | 중상 (시각적 안정성) |
| **P1 (주요)** | **전체** | **어색한 텍스트 줄바꿈(`keep-all` 적용) 및 컬럼 헤더 명칭 한글/축약 표준화** | 중상 (UI 완성도) |
| **P2 (개선)** | **Alerts** | **상단 요약 카드 추가(일관성) 및 출처/레벨별 필터링 기능 강화** | 중 (탐색 효율) |
| **P2 (개선)** | **Interface Lab** | **실행/수신 패널 오버레이 구조 개선 및 불필요한 내부 라벨/설명문 정돈** | 중 (조작 동선) |

---

## 3. 공통 레이아웃 및 반응형 검수 결과

### 3.1. 화면 높이(Vertical Space) 낭비 및 스크롤 과다
- **현상**:
  - 대부분의 모니터링 페이지(Topics, Services, Actions, Nodes)가 **[상단 네비] + [페이지 안내문구] + [요약 카드 5개] + [Alert 리스트 카드] + [검색/필터] + [테이블]** 형태로 수직 적층되어 있습니다.
  - 1440×900 해상도에서는 페이지 진입 시 테이블이 전혀 보이지 않거나 1행만 노출되어, 사용자가 반드시 마우스 휠을 아래로 굴려야 메인 데이터를 탐색할 수 있습니다.
- **개선안**:
  - **Alert 리스트 카드 컴팩트화**: Alert가 있을 때 기본 1~2줄 요약 배너로 표시하거나, 접이식(Accordion/Drawer) 형태로 기본 높이를 대폭 축소.
  - **요약 카드 패딩 및 높이 축소**: 카드의 수직 padding(기존 16~20px → 10~12px) 및 숫자/라벨 폰트 크기 미세 조정을 통해 높이를 30% 이상 절감.
  - **페이지 안내 문구 숨김/툴팁화**: "기본 화면은 운영 시 먼저 확인할..." 등의 긴 설명 문구를 제목 우측의 작은 `ⓘ` 툴팁으로 전환.

### 3.2. 우측 상세 패널(Detail Panel) 오픈 시 반응형 깨짐
- **현상**:
  - 테이블 행 클릭 시 우측에 390px 고정 상세 패널이 등장하면서 좌측 테이블 영역 너비가 줄어듭니다.
  - 이때 테이블 헤더 명칭이 긴 경우(`PUBLISHER NODE 수 (DASHBOARD 제외)`, `마지막 REQUEST/RESPONSE`, `마지막 응답 시간`, `마지막 호출`) 글자가 겹치거나(`마지막 응답 시간 마지막 호출`), 테이블 하단에 가로 스크롤바가 발생합니다.
- **개선안**:
  - 컬럼 헤더 텍스트 축약 및 표준화:
    - `PUBLISHER NODE 수 (DASHBOARD 제외)` ➔ `Pub 노드` (마우스 호버 툴팁으로 전체 설명 제공)
    - `SUBSCRIBER NODE 수 (DASHBOARD 제외)` ➔ `Sub 노드`
    - `SERVER NODE 수 (DASHBOARD 제외)` ➔ `Server`
    - `CLIENT NODE 수 (DASHBOARD 제외)` ➔ `Client`
    - `마지막 REQUEST / RESPONSE` ➔ `최근 요청 / 응답`
  - 테이블 컬럼 최소 너비(`min-width`) 재설정 및 상세 패널 오픈 시 덜 중요한 컬럼(예: JSON preview 일부)의 너비 유연화.

### 3.3. 상세 패널 내부의 중첩 스크롤(Double Scroll)
- **현상**:
  - 우측 상세 패널 내부에서 `최근 데이터 로그 (100)`를 펼치면 패널 전체 높이가 길어지며, [패널 자체 스크롤바]와 [로그 스트림 내부 스크롤바]가 나란히 생기는 이중 스크롤이 발생합니다.
  - 또한 로그가 펼쳐지면 하단의 핵심 진단 정보인 `Topic QoS`, `연결 Node` 등이 화면 밖으로 완전히 밀려납니다.
- **개선안**:
  - 상세 패널 내부를 상단 **[기본 정보 & QoS 요약 고정]** + 하단 **[탭 구조 (로그 / 연결 노드 / 상세 메타)]**로 개편하여 한 화면에서 스크롤 없이 주요 정보를 전환할 수 있도록 개선.

---

## 4. 페이지별 정밀 검수 결과

### 1) Overview (`/overview`)
- **실제 화면 문제점**:
  1. **텍스트 줄바꿈 파편화**: 1440×900에서 상단 카드의 제목이 `Node 미` / `리보기`, `Topic 미` / `리보기` 처럼 한글 단어 중간에서 쪼개져 가독성이 크게 저하됨.
  2. **하단 차트 잘림**: 하단의 `상태 분포` 막대 그래프와 테이블이 1440×900 뷰포트에서 아래로 잘려 한눈에 들어오지 않음.
  3. **헤더 중복 정보**: 최상단 헤더에 `백엔드: 연결됨`, `실시간 연결됨`, `마지막 갱신`, `실시간 갱신` 등 상태 표시가 다소 산만하게 흩어져 있음.
- **개선 권장사항**:
  - 카드 제목 CSS에 `white-space: nowrap` 또는 `word-break: keep-all` 적용.
  - 상단 카드와 하단 차트 간의 간격을 조절하여 1440×900 해상도에서 스크롤 없이 1화면 대시보드(Single-Screen Dashboard)로 완성.

---

### 2) Topics (`/topics`)
- **실제 화면 문제점**:
  1. **필터 버튼 과밀**: 상단에 10개의 필터 버튼(`숨김 포함`, `주요 항목`, `전체`, `대기 중`, `정상`, `주의`, `오류`, `미수신`, `구독자 없음`, `미지원`)이 한 줄에 빽빽하게 나열되어 시각적 노이즈가 심함.
  2. **JSON 값 말줄임**: `마지막 값` 컬럼에 `{"linear":{"x":0.16,"y":0,"z":0},"angular":{"x"...` 형태로 길게 출력되어 실제 주요 값(수치)을 확인하기 어려움.
  3. **헤더 텍스트 비대**: `PUBLISHER NODE 수 (DASHBOARD 제외)` 헤더가 3줄을 차지함.
- **개선 권장사항**:
  - 필터 버튼 그룹화: [전체 / 주요 / 정상 / 주의·오류] 기본 탭 + [상세 상태 필터 드롭다운]으로 간소화.
  - `마지막 값` 컬럼은 대표 필드 요약(예: `Twist: x=0.16, z=2.84`) 또는 간결한 배지 형태로 표시하고 클릭 시 모달로 전체 JSON 확인.
  - 컬럼 헤더를 `Pub 노드`, `Sub 노드`로 축약.

---

### 3) Services (`/services`)
- **실제 화면 문제점**:
  1. **컬럼 헤더 겹침 버그**: 우측 상세 패널 오픈 시 `마지막 응답 시간`과 `마지막 호출` 컬럼 헤더 텍스트가 겹치는 렌더링 결함 발생.
  2. **영문/한글 혼용**: `마지막 REQUEST`, `마지막 RESPONSE` 등 컬럼명에 영문 대문자가 직관적이지 않게 섞여 있음.
  3. **페이지 간 필터 불일치**: Topics 페이지와 달리 필터 버튼이 4개(`주요 항목`, `대기/오류`, `전체`, `내부/관리 포함`)뿐이며, 버튼 순서와 스타일 규칙이 상이함.
- **개선 권장사항**:
  - `마지막 REQUEST/RESPONSE` ➔ `최근 요청 / 최근 응답`으로 한글화.
  - 테이블 컬럼 헤더 `white-space: nowrap` 및 최소 폭 보장으로 겹침 버그 제거.

---

### 4) Actions (`/actions`)
- **실제 화면 문제점**:
  1. **상세 패널 상단 고정 안내문 과다**: 행 선택 시 열리는 상세 패널 상단에 "결과 조회 정책...", "아직 관찰된 Goal이 없어..." 등 2개의 안내 텍스트 박스가 약 150px 이상을 차지하여 정작 중요한 QoS/상태 정보가 아래로 밀림.
  2. **가로 스크롤 발생**: 상세 패널이 열리면 좌측 테이블의 `마지막 실행 시간`, `마지막 GOAL 시각` 컬럼이 잘려 가로 스크롤을 해야만 확인 가능.
  3. **대문자 영문 혼용**: `마지막 GOAL 상태`, `마지막 FEEDBACK`, `마지막 RESULT`, `마지막 GOAL 시각` 등.
- **개선 권장사항**:
  - 상세 패널의 긴 정책 안내문은 접이식 또는 작은 `ⓘ` 아이콘 툴팁으로 변경.
  - 컬럼명 한글화 (`Goal 상태`, `피드백`, `결과`, `소요 시간`, `실행 시각`).

---

### 5) Nodes (`/nodes`)
- **실제 화면 문제점**:
  1. **테이블 100% 가림 현상**: 상단 요약 카드 + `Node Alert` 5개가 세로로 길게 나열되면서, 1440×900 해상도에서 메인 노드 테이블이 화면 아래로 완전히 밀려남.
  2. **6연속 영문 대문자 컬럼 헤더**: `TOPIC PUB`, `TOPIC SUB`, `SERVICE SERVER`, `SERVICE CLIENT`, `ACTION SERVER`, `ACTION CLIENT`가 나란히 배치되어 표가 번잡해 보임.
  3. **상세 패널 정보 분산**: 상세 패널에 네임스페이스(`/`), 전체 이름(`/amcl`), 이름(`amcl`)이 중복 표시되고, Topic/Service/Action 연결 정보가 모두 아코디언으로 닫혀 있어 한 번에 관계 파악이 어려움.
- **개선 권장사항**:
  - `Node Alert` 영역을 최대 높이 제한(Max-height) 및 내부 스크롤 또는 1줄 배너로 축소하여 테이블 첫 진입 가시성 확보.
  - 컬럼 헤더를 통일감 있게 축약 (`Topic 발행/구독`, `Service 서버/클라이언트`, `Action 서버/클라이언트` 또는 아이콘+라벨).

---

### 6) Visualization (`/visualization`)
- **실제 화면 문제점**:
  1. **다이어그램 블록 겹침(Overlap)**: 노드 카드를 클릭했을 때 나타나는 연결 그래프에서 중앙의 노드와 좌측 토픽, 하단 서비스 박스가 서로 겹쳐서 텍스트와 라인이 가려짐.
  2. **헤더 상태 중복**: 상단 헤더의 `실시간 연결됨` 외에 본문 우측 상단에 `● WebSocket 실시간 연결`이 또 노출됨.
  3. **상단 툴바 과밀**: 필터 버튼, 검색창, 안내 텍스트(`Shift + 드래그: ...`), 컨트롤 버튼(`화면 맞춤`, `배치 초기화`, `전체 Graph`, `새로고침`)이 한 줄에 몰려 있어 검색창 플레이스홀더(`Node 이름 또는 namespace 검...`)가 잘림.
  4. **노드 카드 텍스트 줄바꿈**: 노드 목록 그리드에서 이름이 긴 노드(`/global_costmap/global_costmap`)의 글자가 중간에 잘려 가독성 저하.
- **개선 권장사항**:
  - React Flow 그래프 레이아웃 알고리즘 간격(Node Separation / Rank Separation) 확대 설정.
  - 상단 툴바를 [필터/검색 영역]과 [뷰 조작 버튼군]으로 2단 분리하거나 그룹화.
  - 중복된 WebSocket 상태 표시 제거.

---

### 7) Alerts (`/alerts`)
- **실제 화면 문제점**:
  1. **요약 카드 부재 (UI 불일치)**: 다른 모든 페이지(Overview, Topics, Services, Actions, Nodes, Vis, Interface Lab)는 상단에 요약 카드가 있으나 Alerts 페이지에만 카드가 없어 레이아웃 일관성이 깨짐.
  2. **검색/필터 기능 불균형**: `현재 Alert` 탭에는 검색/필터가 전혀 없고, `이전 Alert` 탭에만 `이름 검색` 인풋이 존재함.
  3. **필터 부재**: 출처별(Topic, Service, Node, Action) 또는 레벨별(경고, 오류, 심각) 필터 버튼이 없어 알림 목록이 많을 때 탐색이 어려움.
  4. **버튼 텍스트 불일치**: `현재 Alert 삭제` vs `이력 삭제`.
- **개선 권장사항**:
  - 상단 요약 카드 추가: `활성 Alert 수`, `경고 수`, `오류 수`, `해결된 Alert(24h)`.
  - 공통 검색창 및 출처/레벨 다중 필터 칩(Filter Chips) 제공.

---

### 8) Domains (`/domains`)
- **실제 화면 문제점**:
  1. **입력 필드 가로 폭 과다**: 0~232 정수 하나를 입력하는 인풋 필드가 1920px 화면 전체 너비(약 1600px)를 차지하여 시각적으로 매우 부자연스럽고, `추가` 버튼까지의 마우스 이동 거리가 과도하게 멂.
  2. **도메인 목록의 광활한 여백**: `Domain 0`, `Domain 1` 등의 목록 행이 화면 끝까지 늘어나 있어 상태 점(`● 감시 중`)과 우측 `삭제` 버튼 사이가 1000px 이상의 빈 공간으로 벌어져 있음.
- **개선 권장사항**:
  - 컨테이너 최대 너비 제한 (`max-width: 800px` 또는 `960px` 중앙 정렬 카드 레이아웃).
  - 입력창과 `추가` 버튼을 인라인 컴팩트 폼으로 재배치 (폭 200~300px 수준).

---

### 9) Interface Lab (`/interface-lab`)
- **실제 화면 문제점**:
  1. **상단 컨트롤 누적으로 인한 목록 가림**: 안내문, 요약 카드(4개), 관리 버튼 패널(10개 버튼), 검색창, 탭(5개)이 세로로 5단 적층되어 메인 Interface 목록이 첫 행만 간신히 보임.
  2. **실행 패널 열림 시 화면 덮임**: `Topic 실행` / `Service 실행` 등을 누르면 상단에 2열(실행 + 수신) 대형 패널이 펼쳐져 아래 목록이 완전히 화면 밖으로 밀려남.
  3. **개발자용 변수명 및 영문 노출**:
     - `Message import됨만 보기 2/2`
     - `Message full_type`
     - `Service response receive history · 0개`
     - `Action feedback/result receive history · 0개`
  4. **타입 직접 등록 패널 인풋 너비 과다**: `full type` 입력창과 `타입 등록` 버튼이 화면 전체 너비로 늘어남.
- **개선 권장사항**:
  - 상단 관리 패널을 컴팩트한 툴바 형태로 정리.
  - 실행/수신 패널 오픈 시 화면을 밀어내는 인라인 방식 대신 모달 또는 우측 슬라이드 드로어(Drawer) 방식으로 목록과 분리.
  - 텍스트 정제: `Message full_type` ➔ `메시지 타입`, `Service response receive history` ➔ `서비스 응답 수신 이력`.

---

## 5. 최종 결론 및 권장 실행 단계

본 검수는 실제 동작 중인 ROS2 Dashboard의 1920×1080 및 1440×900 해상도 화면을 전수 분석하여 작성되었습니다.  
현재 시스템은 백엔드 및 ROS 통신 로직이 매우 안정적으로 동작하고 있으므로, 위의 **P0 및 P1 우선순위 항목**을 중심으로 레이아웃 CSS 및 컴포넌트 구조를 개선하면 대시보드의 실무 사용성과 완성도가 비약적으로 향상될 것입니다.

**권장 후속 작업:**
1. **1단계 (레이아웃 & CSS 개선)**: 카드/Alert 높이 축소, `keep-all` 줄바꿈 적용, 테이블 컬럼 헤더 텍스트 축약 및 가로 스크롤 버그 수정.
2. **2단계 (컴포넌트 구조 개선)**: 상세 패널 내부 탭화, Visualization 그래프 레이아웃 간격 조정, Domains/Interface Lab 폼 너비 제한.
3. **3단계 (일관성 & 텍스트 정제)**: Alerts 페이지 요약 카드 및 필터 추가, 개발자 영문 라벨 한글화.
