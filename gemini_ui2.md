# ROS2 Dashboard UI/UX 2차 정밀 재검수 보고서

**검수 일시:** 2026-08-26  
**검수 환경:** 실제 장비 Nginx HTTPS (`https://192.168.1.123/`), Chrome Headless (1920×1080 Full HD & 1440×900 Laptop 해상도)  
**검수 대상 페이지:** Overview, Topics, Services, Actions, Nodes, Visualization, Alerts, Domains, Interface Lab (총 9개 화면 전수 재검수)  
**검수 목적:** 최근 UI 수정사항 반영 결과 평가 및 실제 화면에 잔여하는 UX/UI 결함 도출

---

## 1. 최근 수정사항 반영 결과 평가 (Resolved)

이전 1차 검수 대비 최근 패치가 적용되어 다음 항목들이 확실하게 개선되었습니다:

1. **Alert 영역 컴팩트화 및 가독성 대폭 향상**:
   - Topic/Service/Action/Node 전용 탭에서 Alert가 **3열 그리드 (배지, `이름 · D99`, 메시지)**로 정돈됨.
   - 기본 3건 노출 + `펼치기 (N)` / `접기` 토글이 적용되어 테이블 가림 현상이 크게 완화됨.
   - 배지 크기 및 폰트 크기가 약 30% 확대되어 가독성이 우수해짐.
2. **상세 패널(Detail Panel) 아코디언 및 스크롤 구조 정돈**:
   - `상태 요약`만 기본 펼침, 나머지 섹션(QoS, 연결 정보, 실행/측정 등)은 기본 접힘으로 통일되어 패널 첫 화면 가시성 확보.
   - Action 상세 패널 상단의 과도한 고정 정책 안내문(2개)이 제거되고 요약 정보가 상단에 바로 노출됨.
   - Camera Topic에서 큰 보기 모달 오픈 시 [좌측 이미지 + 우측 최근 데이터 로그]의 2열 배치가 정상 작동함.
3. **테이블 컬럼 헤더 축약 및 1920×1080 레이아웃 안정화**:
   - `PUB 노드`, `SUB 노드`, `SERVER`, `CLIENT`로 컬럼 헤더가 축약되어 1920px 화면에서 상세 패널 오픈 시 컬럼 헤더가 겹치던 결함이 해소됨.
4. **전체 다크 테마 일관성(Navy/Cyan Theme) 확보**:
   - 배경의 미세 grid 패턴, cyan 보더, React Flow 컨트롤의 다크 톤 적용으로 대시보드 전체의 시각적 완성도가 향상됨.

---

## 2. 잔여 문제점 우선순위 로드맵 (Remaining Issues)

| 우선순위 | 영역 | 핵심 문제 및 개선안 | 영향도 |
| :--- | :--- | :--- | :--- |
| **P0 (긴급)** | **Services / Actions** | **1440×900 해상도에서 상세 패널 오픈 시 좌측 테이블 가로 스크롤 발생 버그** | 최상 (노트북 데이터 가독성) |
| **P0 (긴급)** | **Overview** | **1440×900 해상도에서 요약 카드 제목 한글 쪼개짐 (`Node 미` / `리보기`)** | 상 (첫인상 및 한글 가독성) |
| **P1 (주요)** | **Topics/Services/Actions/Nodes** | **상단 누적 높이(430~460px)로 인한 1440×900 테이블 첫 화면 가시성 부족** | 상 (작업 효율성) |
| **P1 (주요)** | **Domains** | **단일 숫자 입력창 및 목록 행의 극단적인 가로 폭(1600px+) 불균형** | 중상 (시각적 균형감) |
| **P1 (주요)** | **Visualization** | **상단 툴바 과밀(14개 이상 요소) 및 중복된 WebSocket 상태 표시** | 중상 (툴바 사용성) |
| **P1 (주요)** | **Interface Lab** | **개발자용 변수명 라벨 노출 및 실행 패널 오픈 시 목록 100% 밀림** | 중상 (조작 직관성) |
| **P2 (개선)** | **Alerts** | **상단 요약 카드 부재(UI 불일치) 및 `현재 Alert` 탭 검색/필터 기능 부재** | 중 (탐색 편의성) |
| **P2 (개선)** | **공통 테이블** | **컬럼 헤더 영문 대문자 혼용 (`마지막 REQUEST/RESPONSE` 등) 한글 통일** | 중 (텍스트 일관성) |

---

## 3. 세부 잔여 문제점 및 개선 권장사항

### 3.1. 🔴 P0 (긴급 / 사용성 결함)

#### 1) 1440×900 해상도에서 Services / Actions 상세 패널 오픈 시 좌측 테이블 가로 스크롤 발생
- **현상**:
  - Topics 화면은 컬럼 축약으로 가로 스크롤이 사라졌으나, **Services와 Actions 화면**은 1440×900 해상도에서 우측 상세 패널(390px)이 열리면 좌측 테이블에 **가로 스크롤바가 발생**합니다.
  - Services: `마지막 REQ...` 뒷부분 컬럼(`마지막 RESPONSE`, `마지막 응답 시간`, `마지막 호출`)이 가려짐.
  - Actions: `마지막 FEEDBACK`, `마지막 RESULT`, `마지막 소요시간/시각` 컬럼이 가려져 가로 스크롤을 해야만 확인 가능.
- **원인**: Services/Actions 테이블 컬럼들의 고정 `min-width` 합계가 상세 패널 오픈 시 줄어든 테이블 영역(약 800px)보다 큼.
- **개선안**:
  - Services: `마지막 REQUEST/RESPONSE` 컬럼 너비 축소 또는 요약 배지화, 덜 중요한 컬럼의 flex 축소 비율 조정.
  - Actions: `마지막 FEEDBACK/RESULT` 컬럼 폭 최적화 및 `min-width` 유연화.

#### 2) Overview 요약 카드 제목 한글 쪼개짐 (1440×900)
- **현상**:
  - 1440×900 랩탑 해상도에서 Overview 상단 카드의 제목이 `Node 미` / `리보기`, `Topic 미` / `리보기` 처럼 단어 중간에서 2줄로 깨져 표시됩니다.
- **원인**: 카드 제목 래퍼에 `word-break: keep-all` 또는 `white-space: nowrap`가 미적용되어 너비가 좁아질 때 글자 단위 줄바꿈 발생.
- **개선안**: 카드 헤더 타이틀에 `word-break: keep-all`, `white-space: nowrap` 적용.

---

### 3.2. 🟡 P1 (주요 / UX 완성도)

#### 1) 상단 카드 + Alert 누적 수직 높이로 인한 테이블 가시성 (1440×900)
- **현상**:
  - Alert가 기본 3개로 축소되었으나, **[상단 네비바] + [페이지 설명문] + [요약 카드 5개] + [Alert 박스]**가 수직으로 누적되어 여전히 상단에서 약 430~460px의 높이를 점유합니다.
  - 1440×900 화면에서 테이블이 상단 3~4행만 보이고 아래쪽이 잘려, 페이지 진입 후 항상 스크롤이 필요합니다.
- **개선안**:
  - 요약 카드의 상하 padding(현재 약 16px → 10~12px) 및 숫자 폰트 크기 미세 축소.
  - "기본 화면은 운영 시 먼저 확인할..." 등의 긴 페이지 설명문을 타이틀 우측 `ⓘ` 툴팁으로 전환하여 수직 공간 30~40px 추가 확보.

#### 2) Domains 페이지의 극단적인 가로 폭 불균형 (1920×1080)
- **현상**:
  - 단일 숫자(0~232)를 입력하는 `ROS Domain IDs` 인풋 필드가 1920px 전체 가로 폭(약 1600px)을 차지하고, `추가` 버튼이 오른쪽 끝에 멀리 떨어져 있습니다.
  - 감시 중 Domain 목록도 전체 너비로 늘어나 `Domain 0` 라벨과 `삭제` 버튼 사이에 1000px 이상의 불필요한 빈 공간이 발생합니다.
- **개선안**:
  - 컨테이너에 `max-width: 800px` (또는 `900px`) 중앙 정렬 또는 폼 영역 폭을 250~300px 수준으로 컴팩트화.

#### 3) Visualization 상단 툴바 과밀 및 WebSocket 중복 상태
- **현상**:
  - 노드 다이어그램 상단 툴바에 필터 버튼(3개), 검색창, 필터 칩(5개), 조작 안내문(`Shift + 드래그: ...`), 컨트롤 버튼(4개) 등 총 14개 이상의 요소가 한 줄에 배치되어 검색창 플레이스홀더(`연결된 Topic, Service, Action...`)가 잘립니다.
  - 최상단 네비바에 이미 `● 실시간 연결됨`이 표시되고 있으나, 본문 우측 상단에 `● WebSocket 실시간 연결`이 중복 노출됩니다.
- **개선안**:
  - 중복된 본문 WebSocket 상태 라벨 제거.
  - 상단 툴바를 [필터/검색 영역]과 [뷰 조작 버튼군]으로 시각적 그룹 분리.

#### 4) Interface Lab 개발자용 변수명 노출 및 인라인 패널 화면 밀림
- **현상**:
  - `Message full_type`, `Message import됨만 보기 2/2` 등 개발자용 프로퍼티 변수명이 사용자 UI 라벨로 그대로 노출됩니다.
  - `Topic 실행` / `Service 실행` 클릭 시 상단에 거대한 2열 split 패널이 인라인으로 열려 하단의 메인 Interface 목록 테이블이 완전히 화면 아래로 밀려납니다.
- **개선안**:
  - 라벨 정제 (`Message full_type` ➔ `메시지 타입`, `import됨만 보기` 등으로 직관화).
  - 실행/수신 패널을 탭 전환 또는 우측 드로어/모달 형태로 구성하여 메인 목록과의 공존성 개선.

---

### 3.3. 🔵 P2 (개선 / 일관성 및 편의성)

#### 1) Alerts 페이지 레이아웃 일관성 및 다중 필터 부재
- **현상**:
  - 다른 8개 페이지와 달리 Alerts 페이지만 상단 요약 카드(KPI 카드)가 없어 페이지 진입 시 레이아웃 일관성이 떨어집니다.
  - `현재 Alert` 탭에는 검색/필터 기능이 전혀 없어, 알림이 수십 건 발생했을 때 출처별(Node/Topic/Service) 또는 레벨별(경고/오류) 빠른 조회가 어렵습니다.
  - 탭별 삭제 버튼 라벨이 불일치합니다 (`현재 Alert 삭제` vs `이력 삭제`).
- **개선안**:
  - 상단 요약 카드 추가 (`활성 Alert`, `경고`, `오류`, `24시간 해결 이력`).
  - `현재 Alert` 탭에 출처/레벨 필터 칩 및 검색창 적용.
  - 삭제 버튼 라벨을 `알림 삭제` / `이력 전체 삭제` 등으로 통일.

#### 2) 컬럼 헤더 명칭의 영문 대문자 혼용 정제
- **현상**:
  - Services: `마지막 REQUEST`, `마지막 RESPONSE`
  - Actions: `마지막 GOAL 상태`, `마지막 FEEDBACK`, `마지막 RESULT`, `마지막 GOAL 시각`
  - Nodes: `TOPIC PUB`, `TOPIC SUB`, `SERVICE SERVER`, `SERVICE CLIENT` 등
- **개선안**:
  - 한글 표준 명칭으로 일괄 통일 (`최근 요청`, `최근 응답`, `Goal 상태`, `피드백`, `결과`, `발행 Topic`, `구독 Topic` 등).

---

## 4. 최종 결론

최근 수정 패치를 통해 **Alert 가독성, 상세 패널 기본 접힘 구조, Camera 모달 2열 배치, 1920px 테이블 헤더**가 매우 완성도 높게 개선되었습니다.

차후 코드 수정 작업 시에는 위의 **P0(Services/Actions 테이블 가로 스크롤 및 Overview 줄바꿈) 및 P1(상단 여백 최적화, Domains 폭 제한)** 항목을 우선적으로 반영하면 대시보드의 실무 사용성이 극대화될 것입니다.
